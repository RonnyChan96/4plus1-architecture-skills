interface:
  display_name: "Architecture Consistency Check"
  short_description: "Review 4+1 model consistency rules."
  default_prompt: "Use $architecture-consistency-check to review this 4+1 architecture model for consistency issues."
policy:
  allow_implicit_invocation: true
